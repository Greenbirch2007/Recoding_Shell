

终端打印、算术运算、常用变量(命令清单)

echo
printf
let
expr
bc
set






常用变量

结合不同的引导为变量赋值

双引号“”：允许通过$符号引用其他变量值
单引号'':禁止引用其他变量值，$视为普通字符
反撇号``:将命令执行的结果输出给给变量


用户自定义变量

设置变量的作用范围　　export
清除用户自定义变量　　　unset

环境变量

环境变量配置文件

全局配置文件：/etc/profile
用户配置文件：~/.bash_profile
查看环境变量

set命令可以查看所有的shell变量，其中包括环境变量
